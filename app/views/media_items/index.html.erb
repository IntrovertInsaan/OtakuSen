<div class="w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">

  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold tracking-tight text-gray-900">My Collection</h1>
    <%= link_to "Add New Item", new_media_item_path, class: "..." %>
  </div>

  <div class="mb-8 flex items-center flex-wrap gap-2">
    <%# ... filter code remains here ... %>
  </div>

  <% if notice.present? %>
    <div class="bg-green-100 ...">
      ...
    </div>
  <% end %>

  <%= turbo_frame_tag "results" do %>
    <% if @media_items.any? %>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        
        <%# This single line now renders all your cards %>
        <%= render @media_items %>
        
      </div>
    <% else %>
      <div class="text-center ...">
        ...
      </div>
    <% end %>
  <% end %>
</div>
